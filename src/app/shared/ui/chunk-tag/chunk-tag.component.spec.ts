import { describe, expect, it } from 'vitest';
import { ChunkTagComponent } from './chunk-tag.component';

describe('ChunkTagComponent', () => {
  it('should exist', () => {
    expect(ChunkTagComponent).toBeDefined();
  });
});
