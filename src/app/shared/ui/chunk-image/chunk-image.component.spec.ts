import { describe, expect, it } from 'vitest';
import { ChunkImageComponent } from './chunk-image.component';

describe('ChunkImageComponent', () => {
  it('should exist', () => {
    expect(ChunkImageComponent).toBeDefined();
  });
});
