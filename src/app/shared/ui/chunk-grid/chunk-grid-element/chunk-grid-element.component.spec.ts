import { describe, expect, it } from 'vitest';
import { ChunkGridElementComponent } from './chunk-grid-element.component';

describe('ChunkGridElementComponent', () => {
  it('should exist', () => {
    expect(ChunkGridElementComponent).toBeDefined();
  });
});
