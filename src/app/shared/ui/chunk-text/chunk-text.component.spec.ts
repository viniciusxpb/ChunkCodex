import { describe, expect, it } from 'vitest';
import { ChunkTextComponent } from './chunk-text.component';

describe('ChunkTextComponent', () => {
  it('should exist', () => {
    expect(ChunkTextComponent).toBeDefined();
  });
});
